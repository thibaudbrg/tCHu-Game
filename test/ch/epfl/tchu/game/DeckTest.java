package ch.epfl.tchu.game;

import ch.epfl.tchu.SortedBag;
import org.junit.jupiter.api.Test;

import java.util.List;
import java.util.Random;

class DeckTest {
    /*
    @Test
    void ofTest

    {
        SortedBag.Builder<Card> c = new SortedBag.Builder();
SortedBag d =c.add(2, Card.BLUE).add(Card.GREEN).add(Card.LOCOMOTIVE).add(Card.RED).add(Card.YELLOW).build()
        Deck a = Deck.of(d, new Random(2021));
assertEquals(d,a);


    }

    @Test
    void topCardTest

    {
        SortedBag.Builder<Card> c = new SortedBag.Builder();
        SortedBag d =c.add(2, Card.BLUE).add(Card.GREEN).add(Card.LOCOMOTIVE).add(Card.RED).add(Card.YELLOW).build()
        Deck a = Deck.of(d, new Random(0));
        assertEquals()


    }

*/
}
